sources:
  - id: federation
    files:
      - ../metadata/select-predicates.xml

pipeline:
  - load:
      source: federation
  - sort:
      order_by: //saml:AttributeValue
  - publish:
      output: sort-xpath.txt
