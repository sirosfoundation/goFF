- when x:
  - load:
      entities:
        - https://idp.example.org/idp
        - https://sp.example.org/sp
  - select:
      entities:
        - https://sp.example.org/sp
  - publish:
      output: when-x.txt
- when request:
  - emit application/json
- when accept application/json:
  - emit application/json
