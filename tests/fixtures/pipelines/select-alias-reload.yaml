sources:
  - id: federation
    files:
      - ../metadata/select-predicates.xml
pipeline:
  - load:
      source: federation
  - select as /idp-only:
      role: idp
  - load:
      source: /idp-only
  - publish:
      output: select-alias-reload.txt
