sources:
  - id: federation
    files:
      - ../metadata/select-predicates.xml

pipeline:
  - load:
      source: federation
  - select:
      roles:
        - idp
        - sp
      match: all
  - publish:
      output: roles-all.txt
